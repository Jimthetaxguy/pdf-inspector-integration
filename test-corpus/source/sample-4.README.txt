Public-fixture placeholder.

No source document is distributed in this slot. Add only a synthetic or
redistributable single-page text PDF as `sample-4.pdf`, record its provenance,
and then regenerate the OCR benchmark.
